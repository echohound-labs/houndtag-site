# X1 RPC endpoint the site reads on-chain data from.
# Defaults to https://rpc.mainnet.x1.xyz if unset.
X1_RPC_URL=https://rpc.mainnet.x1.xyz

# Block explorer base URL (used for program + tx links).
# Defaults to https://explorer.x1.xyz if unset.
X1_EXPLORER_URL=https://explorer.x1.xyz
